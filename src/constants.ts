export type Constants = {
    name: string;
    shortName: string;
    description: string;
}

export const constants: Constants = {
    name: "BimSprint",
    shortName: "BimSprint",
    description: "First public transport fortune teller 🔮",
};